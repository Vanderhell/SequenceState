# SequenceState research records

All generators use explicit seeds. `tools/run_experiments.py` is a small deterministic probe; C unit tests and the smoke benchmark are built through CMake. Results are evidence for this repository state, not claims of cryptographic quality or universal sequence memory.
